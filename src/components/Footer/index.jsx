import styles from './Footer.module.css'

const Footer = () => {
    return(
        <footer className={styles.footerWrap}>
            <p className={styles.createTitle}>Create by Fauzi</p>
        </footer>
    )
}

export default Footer;